services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: quiz-backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
    networks:
      - appsec-net

networks:
  appsec-net:
    external: true
