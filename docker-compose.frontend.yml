services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: quiz-frontend
    ports:
      - "3000:8080"
    networks:
      - appsec-net

networks:
  appsec-net:
    external: true
